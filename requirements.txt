# Pinned versions: someone cloning this repo in 2 years gets exactly the
# same results as here. This is the basis of reproducibility.
# Also the last set of versions compatible with Python 3.9.
mne==1.8.0              # all EEG processing: EDF reading, filtering, epochs, CSP
numpy==2.0.2            # matrix computation (an EEG signal IS a channels x time matrix)
scipy==1.13.1           # digital filters used internally by MNE
scikit-learn==1.6.1     # LDA, cross-validation, confusion matrix
matplotlib==3.9.4       # every figure
pooch==1.8.2            # download + cache of PhysioNet files, used by MNE
